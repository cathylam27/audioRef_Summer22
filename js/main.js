// create a module for our project
(() => {
    //create a reference to the main headline (the first h2 element)
    let mainHeadline = document.querySelector('#main-headline');

    // pause the JS execution here
    debugger;
})()
